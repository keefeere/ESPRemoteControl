# InpuDeck: restore BlueZ's address-resolution flag for bonded dual-mode peers.
# Runs once when bluetoothd starts; no discovery or connection requests.
[Service]
ExecStartPost=-/usr/bin/python3 /usr/local/libexec/inpudeck-le-address-resolution.py --adapter hci0
